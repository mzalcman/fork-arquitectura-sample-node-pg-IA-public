# 📓 Bitácora de Prompts — Ejercicio N° ___

> Copiá este archivo por cada ejercicio que entregues. Nombralo, por ejemplo, `entregas/01-bitacora.md`.
> Esta bitácora **es parte de la nota**. Un ejercicio sin bitácora no se corrige.

---

## Datos

- **Alumno/a:** Zalcman Morena y Jamilis Eliel
- **Ejercicio:** N° 01 — Nueva tabla y su CRUD ⭐
- **Fecha:** 06/07/2026
- **Modelo de IA usado:** (ej: ChatGPT, Claude, Gemini, Copilot) Gemini

---

## 1. 🎯 Qué me pidieron

Resumí en 2–3 líneas el objetivo del ejercicio con tus palabras (no copiado del enunciado).

```
El objetivo es aprender a usar la IA para insertar una tabla nueva (materias) en tu proyecto, copiando la misma estructura de código que ya tienen las otras tablas. La idea es que la IA no invente nada raro, el código te quede idéntico al que ya teníamos, y que nosotros podamos verificar y entender la respuesta de la IA.
```

---

## 2. 💬 Mis prompts (en orden)

Pegá **todos** los prompts que usaste, en orden, con la respuesta resumida y qué hiciste con ella. Agregá tantos como necesites.

### Prompt #1

**Lo que escribí:**
```
Prompt 1 - Creación de repository
Actuá como un desarrollador backend senior en Node.js y Express.

Estoy trabajando en un proyecto con arquitectura en capas (controller → service → repository), usando ES modules y la librería 'pg' (sin ORM) a través de una clase intermedia llamada 'DbPg'.
Para mantener la consistencia del proyecto, te paso el código de mi archivo 'db-pg.js' y de 'cursos-repository.js', que es el CRUD más similar al que necesito:

--- CÓDIGO DE db-pg.js --- 
--- CÓDIGO DE cursos-repository.js ---

En base al patrón exacto de 'cursos-repository.js', generá el archivo 'materias-repository.js' para la tabla 'materias' (id, nombre). Necesito que hagas la clase con los métodos asíncronos necesarios para un CRUD completo (getAllAsync, getByIdAsync, createAsync, updateAsync, deleteByIdAsync).

Restricciones:
●	No uses ORM ni manejes el Pool de pg directamente. Delega todo el acceso a datos en la clase DbPg usando sus métodos ('this.db.queryAll', 'this.db.queryOne', 'this.db.queryReturnId', 'this.db.queryRowCount').
●	Usá estrictamente queries parametrizadas ($1, $2, etc.).
●	Mantené los 'console.log de debug tal cual están en el archivo de referencia.

Por ahora solo generá el código de 'materias-repository.js'. No te adelantes con el service ni el controller, te los pediré en el siguiente paso.
Respuesta: Nos redacto todo el código del respositorio, y también nos recomendó hacer herencia para no repetir código. 

```
### Prompt #2

**Lo que escribí:**
```
Prompt 2 - Service
¡Perfecto el repository! Ahora vamos con la siguiente parte. 
Tenés que generar el archivo ‘materias-service.js’ 

Debe seguir exactamente el mismo patrón que ‘cursos-service.js’. Te pego el código del mismo como referencia de estilo:

--- CÓDIGO DE cursos-service.js ---

Por ahora solo generá el service, no hagas el controller todavía.

Respuesta: Nos redacto todo el código del service, y también nos recomendó tener ciertos cuidados a futuro. 
```
### Prompt #3

**Lo que escribí:**
```
Prompt 3 - Controller
Excelente. Ahora vamos por la última parte: el controller.
Generá el archivo ‘materias-controller.js’ con los 5 endpoints del CRUD.

Restricciones:
Seguí exactamente el patrón de ‘cursos-controller.js’ 
Asegurate de devolver los status codes correctos: 200, 201, 404, 400, etc. 

Te pego el controller de referencia:
--- CÓDIGO DE CONTROLLER DE REFERENCIA ---

Respuesta: Nos redacto todo el código del controller.
```

**Por qué necesité este segundo y tercer prompt** (qué falló o faltó en el anterior): Decidimos ir por partes, realizando un archivo por prompt, para poder otorgar un mayor detalle en las instrucciones


**Auto-chequeo de las 5 partes EFSI** (marcá lo que incluiste):
- [X] Rol
- [X] Contexto (¿pegaste código del proyecto?)
- [X] Tarea
- [X] Restricciones
- [X] Iteración

**Qué me devolvió (resumen):**
```
Todo el código, respetando los parámetros y condiciones dadas, siguiendo el estilo de los archivos previamente creados.
```

**¿Me sirvió tal cual, o tuve que corregir/repreguntar?**
```
Nos sirvió tal cual ya que habíamos relaizado los prompts con un alto grado de detalles.
```


## 4. 🐛 Errores o cosas mal que detecté en la respuesta de la IA

> Si ponés "ninguno", probablemente no las viste. **Siempre** hay algo (un import de más, un estilo distinto, un caso borde olvidado, una mala práctica de seguridad).

```
No pudimos detectar ningún error o cosas mal hechas, más allá de que revisamos las advertencias que nos hizo. 
```

---

## 5. ✅ Verificación

Pegá el checklist de verificación del ejercicio y marcá lo que comprobaste **vos** (con qué evidencia: captura de Postman, salida de `npm test`, número de ms, etc.).

```

***Marcá cada ítem cuando lo verifiques vos (no la IA):***

[X] El repository delega el acceso a datos en la clase DbPg (this.db.queryAll/queryOne/...), igual que alumnos-repository.js. No toca el Pool directamente ni crea un Client nuevo por request (de eso se encarga db-pg.js con lazy init).
[X] Las queries usan placeholders $1, $2... (no concatenación de strings → eso sería SQL injection, ver ejercicio 09).
[X] El controller devuelve los status codes correctos: 200, 201 en POST, 404 cuando no existe, 400 en error de input.
[X] El update valida que el id de la URL coincida con el del body (mirá cómo lo hace alumnos-controller.js en el PUT).
[X] El controller está registrado en server.js y los 5 endpoints responden en Postman.
[X] No aparecieron dependencias nuevas en package.json.
```

---

## 6. ✍️ Reflexión (300–600 palabras)

Cubrí: qué proceso seguiste, qué decisiones tomaste y por qué, qué aprendiste, y —lo más importante— **qué corregiste de lo que te dio la IA**. Escribí con tus palabras; esto se contrasta con el oral.

```
Proceso, decisiones y aprendizaje

Decidimos trabajar de forma guiada y capa por capa (Repository --> Service --> Controller) en lugar de pedirle todo el CRUD junto. Tomamos esta decisión para mantener el control total del código y evitar que la IA inventara estructuras raras. Con esto aprendimos que la clave no es codear desde cero, sino saber estructurar un buen prompt (con rol, contexto y restricciones) para obligar a la IA a replicar con exactitud la arquitectura que ya funciona en el proyecto.

Qué corregí de lo que me dio la IA

No corregimos nada de código. Como los prompts fueron súper precisos y con los archivos de referencia correctos, la IA nos devolvió un código impecable que copió a la perfección el patrón de "cursos": usó las queries parametrizadas ($1, $2), mantuvo los console.log de debug y respetó la validación del PUT (comparando el ID de la URL con el del body).Solo registramos las rutas en server.js y chequeamos todo el funcionamiento en Postman. Los 5 endpoints respondieron con sus respectivos status codes (200, 201, 400, 404) y los datos se guardaron en PostgreSQL.

```

---

## 7. 🔗 Adjuntos

- [ ] Link/PDF de la conversación completa con la IA: https://share.gemini.google/EuZBFLtY0Y7P
- [ ] Commit(s) en GitHub: `Hicimos materias-repository, realizado con IA y revisado, y agregamos el endpoint al server.js` - `Hicimos materias-service, realizado con IA y revisado` - `Hicimos materias-controller, realizado con IA y revisado` - `EJ01 terminado; probado con postman` - `Readme EJ01`

