# 📓 Bitácora de Prompts — Ejercicio N° ___

> Copiá este archivo por cada ejercicio que entregues. Nombralo, por ejemplo, `entregas/01-bitacora.md`.
> Esta bitácora **es parte de la nota**. Un ejercicio sin bitácora no se corrige.

---

## Datos

- **Alumno/a:** Zalcman Morena y Jamilis Eliel
- **Ejercicio:** N° 02 — Refactorización del CRUD repetido ⭐
- **Fecha:** 06/07/2026
- **Modelo de IA usado:** (ej: ChatGPT, Claude, Gemini, Copilot) Gemini

---

## 1. 🎯 Qué me pidieron

Resumí en 2–3 líneas el objetivo del ejercicio con tus palabras (no copiado del enunciado).

```
La idea de fondo de todas es la misma: el código común se escribe una sola vez, y cada entidad (alumnos, cursos, materias) solo aporta lo que tiene de distinto (su nombre de tabla y sus columnas). Cambian en cómo logran eso.
```

---

## 2. 💬 Mis prompts (en orden)

Pegá **todos** los prompts que usaste, en orden, con la respuesta resumida y qué hiciste con ella. Agregá tantos como necesites.

### Prompt #1

**Lo que escribí:**
```
Actuá como un desarrollador backend senior en Node.js y experto en patrones de diseño arquitectónico.

Tengo una API REST en Express con arquitectura en capas. Acabo de terminar de armar los repositories para mis tres entidades (`alumnos`, `cursos` y `materias`). Noto que hay muchísima duplicación de código en el armado de las queries SQL básicas (`SELECT * FROM`, `SELECT * WHERE id=`, `DELETE WHERE id=`) entre los distintos archivos. Te paso el código de mis repositories actuales como referencia:

--- CÓDIGO DE cursos-repository.js ---
import Db from './db-pg.js';


export default class CursosRepository {
    constructor() {
        console.log('Estoy en: CursosRepository.constructor()');
        this.db = new Db();
    }


    getAllAsync = async () => {
        console.log(`CursosRepository.getAllAsync()`);
        const sql = `SELECT * FROM cursos`;
        return await this.db.queryAll(sql);
    }


    getByIdAsync = async (id) => {
        console.log(`CursosRepository.getByIdAsync(${id})`);
        const sql = `SELECT * FROM cursos WHERE id=$1`;
        return await this.db.queryOne(sql, [id]);
    }


    createAsync = async (entity) => {
        console.log(`CursosRepository.createAsync(${JSON.stringify(entity)})`);
        const sql = `INSERT INTO cursos (nombre) VALUES ($1) RETURNING id`;
        const values = [entity?.nombre ?? ''];
        return await this.db.queryReturnId(sql, values);
    }


    updateAsync = async (entity) => {
        console.log(`CursosRepository.updateAsync(${JSON.stringify(entity)})`);
        const sql = `UPDATE cursos SET nombre = $2 WHERE id = $1`;
        const values =  [   entity.id,
                            entity?.nombre ?? ''
                        ];
        return await this.db.queryRowCount(sql, values);
    }


    deleteByIdAsync = async (id) => {
        console.log(`CursosRepository.deleteByIdAsync(${id})`);
        const sql = `DELETE FROM cursos WHERE id=$1`;
        return await this.db.queryRowCount(sql, [id]);
    }
}




--- CÓDIGO DE materias-repository.js ---
import Db from './db-pg.js';
{/* Hecho con IA (Gemini) - Revisado por nosotros */}
export default class MateriasRepository {
    constructor() {
        console.log('Estoy en: MateriasRepository.constructor()');
        this.db = new Db();
    }


    getAllAsync = async () => {
        console.log(`MateriasRepository.getAllAsync()`);
        const sql = `SELECT * FROM materias`;
        return await this.db.queryAll(sql);
    }


    getByIdAsync = async (id) => {
        console.log(`MateriasRepository.getByIdAsync(${id})`);
        const sql = `SELECT * FROM materias WHERE id=$1`;
        return await this.db.queryOne(sql, [id]);
    }


    createAsync = async (entity) => {
        console.log(`MateriasRepository.createAsync(${JSON.stringify(entity)})`);
        const sql = `INSERT INTO materias (nombre) VALUES ($1) RETURNING id`;
        const values = [entity?.nombre ?? ''];
        return await this.db.queryReturnId(sql, values);
    }


    updateAsync = async (entity) => {
        console.log(`MateriasRepository.updateAsync(${JSON.stringify(entity)})`);
        const sql = `UPDATE materias SET nombre = $2 WHERE id = $1`;
        const values = [
            entity.id,
            entity?.nombre ?? ''
        ];
        return await this.db.queryRowCount(sql, values);
    }


    deleteByIdAsync = async (id) => {
        console.log(`MateriasRepository.deleteByIdAsync(${id})`);
        const sql = `DELETE FROM materias WHERE id=$1`;
        return await this.db.queryRowCount(sql, [id]);
    }
}


Analizá detalladamente la duplicación de código entre estos repositories. Proponeme 2 estrategias concretas para aplicar el principio DRY (Don't Repeat Yourself):
Estrategia A) Refactorización por Herencia (BaseRepository y uso de `extends`).
Estrategia B) Refactorización por Composición (GenericRepository inyectado en cada clase).

Restricciones:
- Explicá los pros y contras de cada enfoque aplicados estrictamente a mi proyecto.
- Tomá en cuenta que el proyecto utiliza campos de clase con arrow functions (‘getAllAsync = async () => {}’) y analizá el impacto de esto en la herencia.
- No me des el código refactorizado completo todavía. Solo quiero el análisis conceptual y un mini pseudocódigo de ejemplo para cada opción.
- Queda totalmente prohibido el uso de ORMs (como Sequelize o Prisma). Debemos seguir usando SQL crudo con la clase ‘DbPg’.

Evaluá ambas estrategias. Al final de tu respuesta, preguntame cuál prefiero elegir para que en el siguiente paso procedamos con la escritura del código.

```

### Prompt #1

**Lo que escribí:**
```
Elegí la Estrategia A (Herencia mediante BaseRepository) porque considero que es la más limpia y reduce drásticamente las líneas de código repetidas manteniendo la semántica de que cada entidad "es un" repositorio de datos.

1. Generá el archivo `base-repository.js` que contenga los métodos comunes abstractos (`getAllAsync`, `getByIdAsync`, `deleteByIdAsync`) parametrizados por el nombre de la tabla.
2. Generá las versiones refactorizadas de `cursos-repository.js` y `materias-repository.js` heredando de la clase base.

Restricciones:
- No uses ORM. Seguí usando queries parametrizadas ($1) a través de la instancia de `DbPg`.
- La API pública de los repositorios no debe cambiar bajo ningún concepto. Los services y controllers no deben enterarse de esta refactorización; deben seguir llamando a los mismos métodos y recibir las mismas estructuras de datos.
- Mantené la consistencia de estilo: seguí utilizando campos de clase con arrow functions (`=>`) para evitar la pérdida del contexto `this`, a menos que expliques detalladamente que un método requiere la sintaxis tradicional para habilitar el uso de `super`.
- Recordá que interpolar el nombre de la tabla (`FROM ${this.tabla}`) es seguro en este caso porque la tabla la definimos nosotros como desarrolladores en el código (hardcodeada) y no proviene del input del usuario.
- Mantené los `console.log` de debug en cada método para el rastreo de ejecución.

Mostrame el código de los tres archivos (`base-repository.js`, `cursos-repository.js` y `materias-repository.js`) y al final incluí un breve balance comparativo de líneas de código (el antes vs el después) para verificar el impacto de la refactorización.

```


**Auto-chequeo de las 5 partes EFSI** (marcá lo que incluiste):
- [X] Rol
- [X] Contexto (¿pegaste código del proyecto?)
- [X] Tarea
- [X] Restricciones
- [X] Iteración

**Qué me devolvió (resumen):**
```
Todo el código, respetando los parámetros y condiciones dadas, siguiendo el estilo de los archivos previamente creados.
```

**¿Me sirvió tal cual, o tuve que corregir/repreguntar?**
```
Nos sirvió tal cual ya que habíamos relaizado los prompts con un alto grado de detalles.
```


## 4. 🐛 Errores o cosas mal que detecté en la respuesta de la IA

> Si ponés "ninguno", probablemente no las viste. **Siempre** hay algo (un import de más, un estilo distinto, un caso borde olvidado, una mala práctica de seguridad).

```
No pudimos detectar ningún error o cosas mal hechas, más allá de que revisamos las advertencias que nos hizo. 
```

---

## 5. ✅ Verificación

Pegá el checklist de verificación del ejercicio y marcá lo que comprobaste **vos** (con qué evidencia: captura de Postman, salida de `npm test`, número de ms, etc.).

```
...
```

---

## 6. ✍️ Reflexión (300–600 palabras)

Cubrí: qué proceso seguiste, qué decisiones tomaste y por qué, qué aprendiste, y —lo más importante— **qué corregiste de lo que te dio la IA**. Escribí con tus palabras; esto se contrasta con el oral.

```
...
```

---

## 7. 🔗 Adjuntos

- [ ] Link/PDF de la conversación completa con la IA: https://share.gemini.google/EuZBFLtY0Y7P
- [ ] Commit(s) en GitHub: `ejercicio 2 terminado`
